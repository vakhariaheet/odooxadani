inviteUser:
  handler: src/modules/users/handlers/inviteUser.handler
  events:
    - httpApi:
        path: /api/admin/users/invite
        method: POST
        authorizer:
          name: clerkJwtAuthorizer
        swaggerTags:
          - Users
        summary: POST /api/admin/users/invite
        description: inviteUser endpoint
