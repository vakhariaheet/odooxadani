getUserDetails:
  handler: src/modules/users/handlers/getUserDetails.handler
  events:
    - httpApi:
        path: /api/admin/users/{userId}
        method: GET
        authorizer:
          name: clerkJwtAuthorizer
        swaggerTags:
          - Users
        summary: GET /api/admin/users/{userId}
        description: getUserDetails endpoint
