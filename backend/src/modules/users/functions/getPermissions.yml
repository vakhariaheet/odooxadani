getPermissions:
  handler: src/modules/users/handlers/getPermissions.handler
  events:
    - httpApi:
        path: /api/admin/permissions
        method: GET
        authorizer:
          name: clerkJwtAuthorizer
        swaggerTags:
          - Users
        summary: GET /api/admin/permissions
        description: Get system roles and permissions configuration
