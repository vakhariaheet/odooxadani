banUser:
  handler: src/modules/users/handlers/banUser.handler
  events:
    - httpApi:
        path: /api/admin/users/{userId}/ban
        method: POST
        authorizer:
          name: clerkJwtAuthorizer
        swaggerTags:
          - Users
        summary: POST /api/admin/users/{userId}/ban
        description: banUser endpoint
