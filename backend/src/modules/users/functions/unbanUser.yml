unbanUser:
  handler: src/modules/users/handlers/unbanUser.handler
  events:
    - httpApi:
        path: /api/admin/users/{userId}/unban
        method: POST
        authorizer:
          name: clerkJwtAuthorizer
        swaggerTags:
          - Users
        summary: POST /api/admin/users/{userId}/unban
        description: unbanUser endpoint
