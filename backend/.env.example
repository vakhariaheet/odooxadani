# Environment Variables Template
# ============================================
# Copy this file to .env.dev or .env.prod
# and fill in your values
# ============================================

# Clerk Authentication (Required)
# Get these from your Clerk dashboard: https://dashboard.clerk.com/
# Use sk_test_* for development, sk_live_* for production
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here
CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here

# JWT Authorizer Configuration (Required for HTTP API)
# Get Issuer URL from Clerk Dashboard → JWT Templates → Issuer
CLERK_ISSUER_URL=https://your-clerk-instance.clerk.accounts.dev
# Must match 'aud' claim in your Clerk JWT Template
# Usually your frontend URL
CLERK_AUDIENCE=https://your-frontend-url.com

# AWS Configuration (Optional - can use AWS CLI defaults)
AWS_REGION=ap-south-1
PROFILE=default

# Environment (automatically set during deployment)
NODE_ENV=development

# Custom Domain Configuration (Optional)
# Enable custom domains for both HTTP and WebSocket APIs
# CUSTOM_DOMAIN_ENABLED=true
# BASE_DOMAIN=yourdomain.com

# Cloudflare DNS Configuration (Required for custom domains)
# Get these from Cloudflare Dashboard → My Profile → API Tokens
# CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
# CLOUDFLARE_ZONE_ID=your_zone_id_for_yourdomain_com

# WebSocket Configuration (Optional - auto-configured during deployment)
# These are set automatically by serverless.yml, but you can override them
# WEBSOCKET_API_ID=abc123def456
# WEBSOCKET_URL=wss://abc123def456.execute-api.ap-south-1.amazonaws.com/dev

# DevOps Script Configuration (Optional)
# EPIC_BRANCH=main
# SERVERLESS_STAGE=dev
# PROFILE=default